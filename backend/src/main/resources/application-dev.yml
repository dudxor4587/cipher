# dev: 로컬 docker-compose 로 띄운 Postgres 를 그대로 사용 (backend/docker-compose.yml 와 동일 값).
#   docker compose up -d  후  ./gradlew bootRun  (기본 프로필이 dev)
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cipher
    driver-class-name: org.postgresql.Driver
    username: cipher
    password: cipher

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

cipher:
  jwt:
    secret: cipher-dev-secret-key-please-change-me-in-prod-0123456789
  aes:
    key: cipher-dev-aes-key-change-me-in-prod

# SQL 로그는 개발에서만
logging:
  level:
    org.hibernate.SQL: debug

# 웹푸시 VAPID (개발용 키 — prod 는 환경변수)
vapid:
  public:
    key: BAEOSUv4QV0CnjNzJCjCn9EqECu4RYFLg9u9FtZ8hoH5QIGahORl69fpQuAo6WQFix061hyYmpFfeDtQqYsPmCw
  private:
    key: 3goNJm1GBp0882oF1ZDArGdeTVHbuSMoU1SPtPc0ldo
  subject: https://cipher.134.185.100.62.sslip.io
